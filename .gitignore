# Local stack env (secrets/machine-specific). Commit .env.example instead.
.env

# Service operational logs (friendy_chachkalica/service.py -> logs/{train,eval,other}/)
logs/

person_model_test/

# inferlica/benchmark's default output dir (cached .onnx/.engine artifacts + CSVs).
bench_out/

models/

# Bundle-exporter smoke-test output (build artifacts; carried a vendored copy of
# chachak that silently drifted from the real one).
dist/

# Python bytecode (58 .pyc files were tracked; nothing should compile into the repo).
__pycache__/
*.py[cod]

buildnode.tar.gz
trt_build_export.zip
