# Generated by Django 5.2.16 on 2026-07-20 15:10

import django.core.validators
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('training', '0020_alter_experiment_eval_operating_nms_threshold_and_more'),
    ]

    operations = [
        migrations.AddField(
            model_name='experiment',
            name='detector_expand_ratio',
            field=models.FloatField(default=0.1, help_text="Grow each detected person box by this fraction before cropping, to avoid clipping objects that sit at or just past the person's edge (e.g. a hard hat above the head). 0.10 = +10% on width and height (5% per side); 0 crops the detector box exactly. Applied identically at train, val, and test. Larger values pull in more context but risk dragging neighbouring people/clutter back into the crop. Only used by people_detect_first / batch_people.", validators=[django.core.validators.MinValueValidator(0.0)], verbose_name='Person-box expand ratio'),
        ),
        migrations.AlterField(
            model_name='experiment',
            name='detector_checkpoint',
            field=models.CharField(blank=True, help_text='Person-detector checkpoint; required for people_detect_first / batch_people (and any chain that includes them). Runs at train, val, and test: it selects the person crops the model is trained and evaluated on. Objects outside every detected person crop are not seen during training.', max_length=1024),
        ),
        migrations.AlterField(
            model_name='experiment',
            name='pipeline',
            field=models.CharField(blank=True, choices=[('batch_detect', 'batch_detect — tile, detect per tile, merge'), ('people_detect_first', 'people_detect_first — detect people, crop, detect per crop'), ('batch_people', 'batch_people — tile, detect people, crop, detect per crop'), ('chain', 'chain — run several pipelines and merge')], help_text='Run train, val, and test through this chachak pipeline. Training applies the same transform as eval (tile the frame, or crop around detected people). Blank = plain full-frame training and eval.', max_length=32),
        ),
    ]
