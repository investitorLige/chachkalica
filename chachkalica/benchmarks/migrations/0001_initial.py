# Generated by Django 5.2.17 on 2026-08-26 15:56

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    initial = True

    dependencies = [
        ('fleet', '0005_groundingsamrun'),
    ]

    operations = [
        migrations.CreateModel(
            name='BundleBenchmark',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('bundle_path', models.CharField(help_text='Bundle directory, relative to the bundle root in Training settings.', max_length=1024)),
                ('bundle_name', models.CharField(blank=True, max_length=255)),
                ('fmt', models.CharField(blank=True, help_text='onnx or engine.', max_length=16)),
                ('images_path', models.CharField(blank=True, help_text='Explicit image file or directory. Overrides the dataset above.', max_length=1024)),
                ('max_images', models.PositiveIntegerField(default=24, help_text='Frames used, sorted then truncated so two runs on the same source see the same frames. 0 means all of them.')),
                ('warmup', models.PositiveIntegerField(default=30, help_text="Untimed inference calls before each cell. Never zero: an engine's first call carries deserialization.")),
                ('calls', models.PositiveIntegerField(default=200, help_text='Timed inference calls per cell, grown if needed to span the minimum duration.')),
                ('min_duration_s', models.FloatField(default=2.0, help_text="Floor on the throughput loop's wall time. NVML's utilization counter refreshes about once a second, so a shorter loop reports noise.")),
                ('batch_sizes', models.CharField(default='1', help_text="Comma-separated. Each is capped by the artifacts' own batch profile.", max_length=64)),
                ('concurrency', models.CharField(default='1', help_text='Comma-separated worker-thread counts. Each worker loads its own copy of the bundle, so more streams cost more GPU memory.', max_length=64)),
                ('measure_stages', models.BooleanField(default=True, help_text='Add a per-stage pass (preprocess / person / in-between / model / postprocess). Synchronized at each boundary, so it runs separately from the timing passes.')),
                ('skip_bare_model', models.BooleanField(default=False, help_text='Skip the independent polygraphy/onnxruntime reading of the model artifact alone.')),
                ('status', models.CharField(choices=[('queued', 'queued'), ('running', 'running'), ('ok', 'ok'), ('error', 'error')], default='queued', max_length=16)),
                ('last_error', models.TextField(blank=True)),
                ('output_dir', models.CharField(blank=True, max_length=1024)),
                ('results', models.JSONField(blank=True, null=True)),
                ('p50_ms', models.FloatField(blank=True, null=True)),
                ('fps', models.FloatField(blank=True, null=True)),
                ('gpu_mem_mb', models.FloatField(blank=True, null=True)),
                ('contended', models.BooleanField(default=False, help_text='Another process was on the GPU during the run, so these numbers are not comparable with an idle-GPU baseline.')),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('started_at', models.DateTimeField(blank=True, null=True)),
                ('finished_at', models.DateTimeField(blank=True, null=True)),
                ('dataset', models.ForeignKey(blank=True, help_text="Frames come from this dataset's source images unless a path is given below.", null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='bundle_benchmarks', to='fleet.dataset')),
            ],
            options={
                'verbose_name': 'Bundle benchmark',
                'verbose_name_plural': 'Bundle benchmarks',
                'ordering': ['-created_at'],
            },
        ),
    ]
