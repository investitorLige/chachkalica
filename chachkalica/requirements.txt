# Django fleet manager (web + rq worker + webhook receiver).
# The SAM ML backend keeps its own deps in ml_backends/sam/requirements.txt.
Django>=5.2,<6.0
psycopg[binary]>=3.1
rq>=1.16
django-rq>=2.10
requests>=2.31
pyyaml>=6.0
gunicorn>=22.0
python-dotenv>=1.0
whitenoise>=6.6
# Video download (Videos admin tab). Needs ffmpeg on PATH for mp4 merge/convert;
# yt-dlp-ejs + a JS runtime (deno, installed in the image) solve YouTube's player
# JS challenge. That alone isn't enough anymore: YouTube also gates the actual
# googlevideo.com playback fetch behind a PO Token, which yt-dlp doesn't mint
# itself — bgutil-ytdlp-pot-provider is the plugin that fetches one, from the
# companion `yt-pot-server` container (see docker-compose.yml + downloader.py).
yt-dlp>=2024.8
yt-dlp-ejs
bgutil-ytdlp-pot-provider
# Frame extraction ("Extract frames…" action on Videos). Headless build avoids
# pulling in libGL, which this server image has no use for.
opencv-python-headless>=4.9
